spring:
  datasource:
    url: jdbc:mysql://localhost/inventory?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
debug: false
auth:
  signing-token: ILOveGOD
  token-expiration: 1800
  use-random: false
